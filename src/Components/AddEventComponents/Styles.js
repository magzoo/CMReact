import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    map: {
      height: 300,
    },
    marker: {
        height: '10%',
        width: '10%',
    }
});

export default styles;